---
'@eigenpal/docx-editor-agents': patch
---

Move the source folder from `packages/agent-use` to `packages/agents` so it matches the published npm name (`@eigenpal/docx-editor-agents`). The npm package name, version, exports, and import paths are unchanged — no consumer action needed.
